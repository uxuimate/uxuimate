# Apache — serve the custom 404 page for missing URLs (static HTML site root).
# Safe to keep in repo: ignored by GitHub Pages / Netlify; used on typical cPanel / LAMP hosts.
<IfModule mod_alias.c>
  ErrorDocument 404 /404.html
</IfModule>
